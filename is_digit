#include "main.h"
/**
 * is_digit - confirms if a char is digit
 * @c: Char being checked
 *
 * Return: 1 if c is a digit, 0 otherwise
 */
int is_digit(char c)
{
	if (c >= '0' && c <= '9')
		return (1);

	return (0);
}
